{
  "name": "maid-lsp",
  "displayName": "MAID for VS Code",
  "description": "VS Code extension for MAID (Manifest-driven AI Development) with LSP integration, manifest exploration, test execution, and validation features",
  "version": "0.1.2",
  "publisher": "aidrivencoder",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Linters",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "maid",
    "manifest",
    "validation",
    "lsp",
    "language-server",
    "json",
    "test",
    "ide"
  ],
  "activationEvents": [
    "onLanguage:json",
    "onCommand:maid-lsp.checkInstallation"
  ],
  "main": "./out/extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/mamertofabian/vscode-maid.git"
  },
  "bugs": {
    "url": "https://github.com/mamertofabian/vscode-maid/issues"
  },
  "homepage": "https://github.com/mamertofabian/vscode-maid#readme",
  "license": "MIT",
  "contributes": {
    "languages": [
      {
        "id": "json",
        "filenames": [
          "*.manifest.json"
        ]
      }
    ],
    "commands": [
      {
        "command": "maid-lsp.checkInstallation",
        "title": "Check MAID LSP Installation",
        "category": "MAID"
      }
    ],
    "configuration": {
      "title": "MAID",
      "properties": {
        "maid-lsp.path": {
          "type": "string",
          "default": "maid-lsp",
          "description": "Path to the maid-lsp executable. Use 'maid-lsp' if it's in your PATH."
        },
        "maid-lsp.args": {
          "type": "array",
          "default": ["--stdio"],
          "description": "Arguments to pass to maid-lsp"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.74.0",
    "typescript": "^5.0.0",
    "vscode-languageclient": "^9.0.0"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.0"
  }
}
